pipeline {
    agent { label 'java-cicd-agent' }

    stages {

        stage('1. Git Checkout') {
            steps {
                checkout scm
            }
        }

        stage('2. Build') {
            steps {
                sh 'mvn clean package'
            }
        }

        stage('3. Test') {
            steps {
                sh 'echo "test build artifacts"'
            }
        }

        stage('4. Archive Stage') {
            steps {
                sh 'echo "archiving artifacts"'
            }
        }

        stage('5. Deploy Stage') {
            steps {
                sh 'echo "Deploying application"'
            }
        }
    }
}
